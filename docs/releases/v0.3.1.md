# v0.3.1 — M2.9 리뷰 (문서 정합화)

기간: 2026-08-29
상태: 완료. M2.5(Go 1.27 + 플러그인 아키텍처)의 이월 항목 중 "지금 갱신이 필요한 것"을 M3 착수 전에 처리
범위: 코드 변경 없음. 문서만 현행 구조(867ed1d)에 맞춘다. 대상이 전부 gitignore(PRD.md, docs/)라 커밋 없음

## 산출물

### 1. PRD 결정 로그 D11~D13 추가 (필수 지정)

- **D11 플러그인 아키텍처**: Plugin 인터페이스(선언적 계약) + internal/pipeline 접합부 일원화,
  main blank import가 유일한 참조, pipeline.plugin으로 선택. D10을 대체가 아니라 재정리
- **D12 내장 default 플러그인**: 플러그인 코드 없이 동작, yaml 규칙(base 전용) + 일반 관례 폴백,
  'default' 예약어, internal 위치의 세 가지 근거(순환 참조·등록 시점·프레임워크 의미론)
- **D13 도메인 어휘 규칙**: 소속 도메인 기준 어휘, Draft 접미사, 단일 언어 패키지 불변식
- PRD 본문 현행화: §3 구조(plugin/, internal/pipeline 추가), §4.2(pipeline 섹션과 규칙 출처 D11/D12로
  교체, yaml 예시의 pipeline 키 위치 정정), FR-1 5항(검증 대상을 "선택된 플러그인"으로), FR-2 2항
  (BuildChain 조립 주체, 커스텀 단계는 Transformers() 주입)

### 2. self-review-guide.html 현행화 (06750ed 스냅샷 → 867ed1d)

- 메타/스냅샷: 기준 커밋·갱신일, LOC 5,601 / 패키지 7+2 / 테스트 54, 커밋 표에 3건 추가
- §2 데이터 흐름: 체인 3단계를 "플러그인 커스텀 단계"로 일반화, BuildChain/Select 조립 설명 추가,
  의존 방향 다이어그램에 plugin·pipeline 반영, base=입력 vs latest=출력 구분 명시
- §3 결정 표: D10 부분 해소 표시, D11~D13 행과 재검토 질문 추가
- §5 체크포인트: "main / pipeline.go" 섹션을 "main / plugin / internal/pipeline"으로 재작성
  (Select 분기, 커스텀 단계 삽입 위치, var _ 검증), 테스트 공백 항목을 부분 해소로 갱신
- §7 검증: staticcheck/Go 1.27 반영, M2.5 검증(접합부 테스트, dry-run 동작 보존) 추가
- §8 불일치: README 함수명 건 해소 처리 (개인 DB명이 공개 코드 파일명에 남는 선택은 기록),
  exclude "Root 디렉토리" 표현 건은 미해결로 유지
- §9 질문: Q1 답변됨(latest 기록용 유지 + plugin 필드), Q2 부분 해소(buildProperties만 잔존)

## 남은 이월 (M3 이후로)

- backup: Page → NoteDraft (D13 어휘 규칙의 첫 실전 검증), 역변환 체인의 패키지 분리 검토
- 다중 이관 경로 필요 시 설정 jobs 목록 확장 (플러그인 계약 불변)
- README exclude 설명의 "Root 디렉토리" 표현 재확인 (실제 의미는 조상 경로 매칭)
- main의 buildProperties 단위 테스트 (Q2 잔여)
